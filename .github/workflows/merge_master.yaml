on:
  push:
    branches: [ master ]

jobs:
  run:
    name: Get changes in submodules
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        submodules: true